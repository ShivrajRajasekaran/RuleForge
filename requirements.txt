# RuleForge Dependencies
# Core
networkx>=3.3
sqlalchemy>=2.0.35

# AI/LLM
# NOTE: The LLM client (src/generation/llm_client.py) talks to Ollama over its
# REST API using ONLY the Python standard library — no pip install required.
# You just need the Ollama app running locally + a model pulled:
#     ollama pull mistral
# The packages below are optional, for future LangChain-based orchestration:
# langchain>=0.2.16
# ollama>=0.3.3
# chromadb>=0.5.5

# Backend
# fastapi>=0.115.0
# uvicorn>=0.30.6

# Frontend (web dashboard — Module 7)
streamlit>=1.38.0
pandas>=2.0.0

# Testing
pytest>=8.3.3
pytest-cov>=5.0.0
